export interface EstadoTurno {
  id: string;
  codigo: string;
  nombre: string;
  orden: number;
}

export interface EstadoVehiculo {
  id: string;
  codigo: string;
  nombre: string;
  orden: number;
}

export interface ResultadoEvaluacion {
  id: string;
  codigo: string;
  nombre: string;
  orden: number;
}
